<%provide(:title, "Edit user") %>
<%provide(:button_text, 'Save Changes') %>
<%provide(:url_action, user_path(@user)) %>
<h1>Update your profile <%= user_path(@user) %></h1>

<div class ="row">
	<div class="col-md-6 col-md-offset-3">
		<%= render 'form' %>
		<div class="gravatar_edit">
			<%= gravatar_for @user %>
			<a href="http://gravatar.com/emails" target="_blank" rel="nooeper noreferrer">change</a>
		</div>
	</div>
</div>
